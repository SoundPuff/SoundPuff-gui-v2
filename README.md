
  # SoundPuff Music Platform
  Soundpuff Gui

  ## Running the code

  Run `npm install` to install the dependencies.

  Run `npm run dev` to start the development server.

  Run 'npm install axios' 
  